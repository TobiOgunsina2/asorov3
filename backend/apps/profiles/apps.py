from django.apps import AppConfig

#profiles app for user profiles, settings, and preferences. Can be extended to include things like user-created content, social features, etc.
class ProfilesConfig(AppConfig):
    name = 'apps.profiles'
